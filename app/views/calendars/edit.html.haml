%h1
  = "Update " + @calendar.name

= render 'calendar_form'

= link_to 'DELETE CALENDAR', calendar_path(@calendar), :method => :delete, :confirm => "Are you sure you want to destroy this calendar?", :class => "btn btn-danger"
