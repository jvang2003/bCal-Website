%h1 #{@calendar.name}
%ul
	%li
		Name:
		= @calendar.name
	/ I imagine all the data below this is rendered for admin only
	/ When u uncomment the if push everything affected by the if in by an indentation
	/ - if @dept_admin
	%li
		API key:
		= @calendar.email		
	%li
		Visibility:
		= @calendar.visib
		
	%li
		Fee required ? :
		= @calendar.fee_required

	%li
		Building :
		= @calendar.building

	%li 
		Usage :
		= @calendar.usage

	%li
		Department :
		= @calendar.dept


%table#events.table
	%thead
		%tr		
			%th  Event
			%th  Starting
			%th  Ending
			%th  Number of Attendees
	%tbody
		- @events.each do |e|
			%tr
				%td= e.summary
				%td= e.start.dateTime.to_s
				%td= e.end.dateTime.to_s
				%td= e.attendees.size
				/ %td= e.to_s
				/ %td= e.to_s
				/ %td= e.to_s
				/ %td= e.to_s

- if @calendar.access_token == nil
	%a= link_to "Authenticate", controller: "calendar", action: "auth", id: @calendar.id
