/ = form_tag users_path, :method => 'get', :class => 'form-inline form-header' do
/   .form-group
/     = select_tag "role", options_for_select(User.VALID_ROLES.to_a, @selected), :prompt => "Role...", :class => 'form-control'
/   .form-group
/     = button_tag(:type => :submit, :class => 'btn btn-default form-control') do
/       Filter

- if @users
    = simple_table_for @blocked_times, with: BlockedTimesTable
- else
    %h2 No Blocked Times Yet...